<main>
    <header>
        <i class="fal fa-search"></i>
        <div class="filters-container"></div>
        <input type="search" disabled autofocus spellcheck="false" value="{{lastSearch}}"
            placeholder="{{#if first}}Shift + Spacebar to toggle {{/if}}Spotlight Omnisearch">
    </header>
    <section class="no-results">
        <ul id="search-result">
        </ul>
    </section>
</main>