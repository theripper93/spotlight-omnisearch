<main>
    <header>
        <i class="fal fa-search"></i>
        <input type="search" disabled autofocus spellcheck="false"
        value="{{lastSearch}}"
            placeholder="{{#if first}}Shift + Spacebar to toggle {{/if}}Spotlight Omnisearch">
    </header>
    <section class="no-results">
        <ul id="search-result">
        </ul>
    </section>
</main>